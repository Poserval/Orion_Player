name: Build Orion Player APK

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Prepare web directory
        run: |
          # Создаём папку www для веб-файлов
          mkdir -p www
          # Копируем все нужные файлы в www
          cp index.html www/
          cp style.css www/
          cp script.js www/
          cp -r assets www/

      - name: Initialize Capacitor
        run: |
          # Удаляем старую android папку, если есть
          rm -rf android
          # Инициализируем, указывая правильный webDir
          npx cap init OrionPlayer com.orion.player
          # Устанавливаем правильный webDir в конфиге
          sed -i 's/"webDir": "."/"webDir": "www"/' capacitor.config.json

      - name: Add Android platform
        run: |
          npx cap add android
          npx cap copy
          npx cap sync

      - name: Build APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: orion-player-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
