{
  "name": "orion-player",
  "version": "1.0.0",
  "description": "Cosmic media player without ads",
  "main": "index.js",
  "scripts": {
    "build": "npx cap copy && npx cap sync",
    "open": "npx cap open android",
    "build:apk": "cd android && ./gradlew assembleDebug"
  },
  "keywords": ["media", "player", "android", "capacitor"],
  "author": "Orion Team",
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0",
    "@capacitor/filesystem": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
